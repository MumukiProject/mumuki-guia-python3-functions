expectation "`is_weekend` must use `or` with two boolean expressions, i.e. `x == 1 or x == 2` instead of `x == 1 or 2`":
  within `is_weekend`   (! calls or ||  calls or with (nonliteral, nonliteral));