---
rules:
- when:
    these_expectations_failed:
    - previous_of_triple Not:Uses:previous_of_triple
  then: "you are trying to define `previous_of_triple` using... `previous_of_triple`
    itself! \U0001F635 This can be very confusing for the computer, so as a practical
    rule in these early exercises, **never** call a function within itself.\U0001F44D\U0001F44D"
